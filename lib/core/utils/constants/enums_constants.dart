

enum TextSizes {
  small,
  medium,
  large,
}


enum PaymentMethods { cash, card, wallet }

enum OrderStatus { processing, delivered, cancelled }

enum LoginStatus { loggedIn, loggedOut }

enum StatusResponse { success, error, loading }
